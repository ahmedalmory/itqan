<div class="stat-card">
    <i class="bi bi-{{ $icon ?? 'graph-up' }} stat-icon"></i>
    <div class="stat-title">{{ $title ?? '' }}</div>
    <div class="stat-value">{{ $value ?? 0 }}</div>
    <div class="stat-description">{{ $description ?? '' }}</div>
</div>